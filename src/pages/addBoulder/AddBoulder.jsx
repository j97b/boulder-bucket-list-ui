import React from 'react';
import { Typography } from '@material-ui/core';

const AddBoulder = () => {
    return (
        <Typography>Add Boulder Page Placeholder</Typography>
    );
};

export default AddBoulder;